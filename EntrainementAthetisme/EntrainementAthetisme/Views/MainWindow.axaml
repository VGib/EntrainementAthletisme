<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:EntrainementAthetisme.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="EntrainementAthetisme.Views.MainWindow"
        Icon="/Assets/avalonia-logo.ico"
        Title="EntrainementAthetisme">

    <Design.DataContext>
        <vm:MainWindowViewModel/>
    </Design.DataContext>

    <DataGrid Items="{Binding NiveauxDEntrainements}" AutoGenerateColumns="False" >
      <DataGrid.Columns>
        <DataGridTextColumn Header="Nom" Binding="{Binding Label.Name}" ></DataGridTextColumn>
        <DataGridTextColumn Header="Fréquence Cardiaque" Binding="{Binding FrequenceCardiaque}"></DataGridTextColumn>
        <DataGridTextColumn Header="Vitesse" Binding="{Binding VitesseEnMinParKm}" ></DataGridTextColumn>
        <DataGridTextColumn Header="Puissance" Binding="{Binding Puissance}" ></DataGridTextColumn>
      </DataGrid.Columns>

    </DataGrid>
</Window>
